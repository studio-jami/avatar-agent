# Persona picker and audit cleanup

- Added server-side named persona discovery so the UI shows friendly persona choices instead of raw provider IDs.
- Removed provider/debug copy from the avatar console surface.
- Added a PostCSS override for the Dependabot advisory until the upstream Next dependency path updates.
