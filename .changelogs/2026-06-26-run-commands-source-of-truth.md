# Run commands source of truth

Date: 2026-06-26
Type: docs

- Added `docs/operations/commands.md` as the single canonical run/build/verify command reference.
- Updated root and app docs to link to the canonical commands document instead of duplicating command lists.
- Updated development docs to reference the canonical commands document and reduce command drift.